{
  "name": "redcode-project",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm run build",
    "backend": "cd backend && go run main.go",
    "start": "npm run build && npm run backend"
  }
}
